import type React from "react"
import type { Metadata } from "next"
import { Inter } from "next/font/google"
import "./globals.css"

const inter = Inter({ subsets: ["latin"] })

export const metadata: Metadata = {
  title: "Inoora demo schedule",
  description: "Schedule your personalized Inoora demo with our experts",
  keywords: ["demo", "scheduling", "Inoora", "business workflow", "automation"],
    generator: 'v0.dev'
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <head>
        <title>Inoora demo schedule</title>
        <meta name="description" content="Schedule your personalized Inoora demo with our experts" />
        <meta name="viewport" content="width=device-width, initial-scale=1" />
      </head>
      <body className={inter.className}>{children}</body>
    </html>
  )
}
